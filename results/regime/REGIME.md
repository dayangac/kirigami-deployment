# WP7a — the paper-regime population

**Status: IN PROGRESS.** The constructive arms and the pattern import are complete; the
native pass is still running and every native number below is marked with its coverage.
Everything here is a function of `results/regime/regime.csv`, via
`results/regime/summary.txt`, which `julia --project=Kirigami Kirigami/apps/kill_regime.jl --mode aggregate` derives from
that CSV and from nothing held in memory.

*(skeleton — filled in as the passes complete)*
