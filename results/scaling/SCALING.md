# WP7b — the scaling benchmark

**Status: IN PROGRESS.** Every number is a function of `results/scaling/scaling.csv` and
of `results/scaling/fits.csv` (the least-squares exponents, `code/scripts/plot_scaling.py`).

*(skeleton — filled in as the run completes)*
