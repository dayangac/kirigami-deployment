# E1 -- exact Theta_max (T4.2") vs. bisection referee, extended population

Extends K2a (results/kill/KILL_REPORT.md sec. K2a; 187 tightly-embedded designs, all
authored-tiling-derived) to a 3543-design population that spans random Voronoi/
Delaunay/quad-random graphs and both orientation rules (sigma_mc = Eq. (1) relaxation,
sigma_def = K5's deployability-defect greedy search), using the F34-fixed,
tolerance-independent referee (results/core_validation/referee_fix.md; a single
tol_rel = 1e-12 is reported, since referee_fix.md measured it agrees with 1e-9 and 1e-6
on K2a's population).

Code: `Kirigami/apps/kill_e1.jl` (new; does not modify any core/ or method/ source). Three
populations, all filtered to "tightly embedded" exactly as K2a's own
`deployable_population` does (uniformly deployable, no inverted face, no collision at
theta = 0, tol_rel = 1e-12):

- **authored**: `deployable_population()` (`Kirigami/apps/kill_common.jl`) (frozen in `data/corpus/`) called with (samples_per_base=20,
  radius_frac=0.2) and again with (20, 0.35) -- more seeds/samples than K2a's (8, 0.2) --
  over the same 7 authored families x 5 clip radii. sigma_mc is whatever the function
  assigns (checkerboard or Eq. (1) relaxation); a sigma_def row is added per design by
  K5's greedy defect search at the same X, re-solved and re-filtered.
- **random**: `make_graph(id, 100, 800, 1400)` (`Kirigami/src/core/kill_common.jl`) (frozen in `data/corpus/`) -- Voronoi / Delaunay /
  quad-random, K5's exact population call (measured there to give sigma_def a 61.5%
  embedding rate under the *withdrawn* theta=0 predicate; under this run's tighter,
  uniformly-deployable-AND-embedded filter the yield is lower, see below), ids 0-899,
  both sigma rules.
- **jitter**: the 4 split-bearing authored tilings only (hexagons_auto,
  truncated_square_488, snub_square_33434, tiling_3_4_3_12 -- the other 4 authored
  tilings are split-free, STATE.md F30/A3), interior-vertex Gaussian jitter at 12
  log-spaced amplitudes in [0.01, 0.6] median edge lengths (below/around the known
  a* = 0.28/0.32/0.16/0.52) x 15 seeds x 2 sigma rules, plus the a=0 baseline.

Sharded 12x (`Kirigami/scripts/run_e1.sh`); shards `e1_<k>.csv` merged into
`results/final/e1/e1.csv`.

## Headline

- **N designs: 3543**
- **Agreement at 1e-5** (`|Theta_max(T4.2") - bisect| <= 1e-5`): **3543 / 3543 (100.00 %)**
- **Worst gap: 3.822e-09** (all gaps are floating-point-scale, not resolution-limited --
  the referee's 4000-point grid + 50 bisection steps at tol_rel = 1e-12 tracks the exact
  T4.2" interval scan everywhere in this population)

## Certificate soundness (holds => Theta_max >= eps, eps = 0.006, K2a's value)

- Certificate holds on **2150 / 3543** designs.
- **Soundness: 0 / 2150 violations** -- every design the certificate calls valid the
  referee confirms has `Theta_max >= eps`. Matches K2a's and A3's post-F32 finding of 0
  violations, now over 3543 designs instead of 187 (K2a) or the jitter ladder's 6416
  rows (A3).

## Certificate exactness

- Certified **2150** vs. actually `Theta_max >= eps` **2150 / 3543** -- **exact on this
  population**: every design with `Theta_max >= 0.006` is certified, and every certified
  design has `Theta_max >= 0.006`. Consistent with F32's finding that `NOROOT(eps)` is
  exact for `eps` below the first graze and this population's eps (0.006) sits below
  every graze angle measured so far.

## Per-family table

| family (`source`) | N | agree@1e-5 | worst gap | certified | actually >= eps |
|---|--:|--:|--:|--:|--:|
| delaunay (random) | 258 | 258 | 5.934e-12 | 0 | 0 |
| quad_random (random) | 253 | 253 | 4.534e-12 | 0 | 0 |
| voronoi (random) | 50 | 50 | 8.241e-12 | 0 | 0 |
| squares (authored) | 20 | 20 | 0 | 20 | 20 |
| triangles (authored) | 20 | 20 | 0 | 20 | 20 |
| kagome (authored) | 20 | 20 | 0 | 20 | 20 |
| hexagons (authored) | 420 | 420 | 3.822e-09 | 336 | 336 |
| trunc_square (authored) | 420 | 420 | 7.153e-10 | 272 | 272 |
| snub_square (authored) | 420 | 420 | 8.121e-10 | 290 | 290 |
| t3_4_3_12 (authored) | 340 | 340 | 1.198e-10 | 324 | 324 |
| hexagons_auto (jitter) | 342 | 342 | 3.803e-09 | 259 | 259 |
| truncated_square_488 (jitter) | 342 | 342 | 2.580e-09 | 252 | 252 |
| snub_square_33434 (jitter) | 306 | 306 | 4.913e-10 | 193 | 193 |
| tiling_3_4_3_12 (jitter) | 332 | 332 | 5.268e-10 | 164 | 164 |

All 14 families agree at 1e-5 on every design. Only the 3 random-graph families never
certify or reach `Theta_max >= eps`: **random sigma_mc gives 3 tightly-embedded designs
out of 900 ids tried** (2 Voronoi, 1 Delaunay; consistent with K1a's 1/200 injective
sample under Eq. (1) orientation); random sigma_def (K5's defect search) gives 558
tightly-embedded designs (257 Delaunay, 253 quad-random, 48 Voronoi) -- this run's filter
(uniformly deployable AND collision-free at theta=0 under the F34 predicate) admits more
of them than the pre-F34 predicate did, and which designs pass a theta=0 overlap test at
the 1e-12 tolerance is a knife-edge property of the projection, so the random row count
of this table is not a stable quantity -- and every one of those 561 has `Theta_max = 0`
under the exact scan (the 0+ split-duplicate collision K5/K6 already found on random
graphs), so none certify. The authored and jitter rows are identical to the earlier
131-random-row pass of this experiment.

## Wall time

12-way sharded (`Kirigami/scripts/run_e1.sh`): sum of per-design measurement time (exact
T4.2" scan + referee bisection + certificate) across all 3543 rows, 344.2 s.

## Reproducing

```
export PATH=$HOME/.juliaup/bin:$PATH
bash Kirigami/scripts/run_e1.sh            # 12-way sharded; argument = random ids to try (default 900)
# one shard by hand:
julia --project=Kirigami Kirigami/apps/kill_e1.jl --shard 0 --nshards 12 --n-random 900 --out results/final/e1
julia --project=Kirigami/scripts Kirigami/scripts/plot_e1.jl
```

Outputs: `results/final/e1/e1.csv` (3543 rows, merged from `e1_<k>.csv`),
`results/final/e1/e1_gap.png` (log-scale histogram of `|Theta_max(T4.2") - bisect|`).
