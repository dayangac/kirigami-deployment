--- baseline/tuttekiri/code/cpp/utils/Hmesh.cpp	2026-09-03 18:12:39
+++ baseline/native/src_fixed/Hmesh.cpp	2026-09-08 21:18:29
@@ -270,6 +270,16 @@
 
 Hmesh Hmesh::merge_close_verts() const {
   std::vector<Eigen::VectorXd> new_verts;
+  // F24 crash fix (WP5).  A deployed configuration can collapse to zero faces:
+  // merge_close_verts() drops every face whose vertices fuse to <= 2 distinct
+  // ones under the 0.1*avg_edge_len threshold, and the remove_unused_verts()
+  // that follows it then yields a mesh with V.rows() == 0.  Their collision
+  // scan (max_opening_angle_with_collisions) calls merge_close_verts() again on
+  // that mesh; new_verts stays empty and convert::to_eig_mat() evaluates
+  // v[0].size() on an empty vector -- SIGSEGV, EXC_BAD_ACCESS at address 0x8.
+  // Return the empty mesh unchanged instead of building a matrix from nothing.
+  if (V.rows() == 0)
+    return *this;
   std::vector<int> old_to_new(V.rows(), -1);
   double elen = avg_edge_len();
   for (int i = 0; i < V.rows(); i++) {
--- baseline/tuttekiri/code/cpp/geometry/unit_pattern.cpp	2026-09-03 18:12:39
+++ baseline/native/src_fixed/unit_pattern.cpp	2026-09-08 21:18:29
@@ -144,6 +144,14 @@
       } else {
         // Color == 1.
         cur_edge = cur_edge->prev();
+        // F24 crash fix (WP5).  prev() may be a boundary half-edge with no
+        // twin on a patch that make_periodic() could not close; the original
+        // dereferences cur_edge->twin() here unconditionally.  Stop tracing
+        // this hole instead (the hole is simply not closed, so it is not
+        // recorded, which is the same outcome as the !cur_edge->twin() guard
+        // a few lines above).
+        if (!cur_edge->twin())
+          break;
         if (pattern.face_colors[cur_edge->twin()->fi] == 0) {
           shift_x -= cur_edge->shift_x;
           shift_y -= cur_edge->shift_y;
