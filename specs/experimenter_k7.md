# Experimenter K7: kill experiment for R4 (achievable periodic Jacobians / designable Poisson family) + B20 (closed-form second closed angle)

Output: results/kill/k7/ + a K7 section in results/kill/KILL_REPORT.md. All C++ (D3); sharded across cores from the start (12 processes) if any step exceeds ~5 min serial.
Read first: specs/common_preamble.md; code/README.md; derivations/core.md T1 (Y_θ = cos(θ/2)C + sin(θ/2)S, C,S linear in X), T3, T5; notes/paper_2026.md Sec 5.1 (Eq. 11–13: P_θ, J(θ) = P_θ P_0⁻¹, conformal condition) and Sec 5.2 (Eq. 14, fully closed at θ_max); ideas/persona_optimizer.md ideas 1 and 5; ideas/persona_geometer.md G2; ideas/ranking.md R4 and B20; STATE.md F15, F22 (periodic rank), F27–F30.

Population: ≥ 30 periodic patterns: the periodic generators in code/src/core/generators (torus/periodic squares, triangles, hexagons, kagome, snub square, 3.4.3.12, 4.8.8 at 2–3 cell sizes) plus periodic random patterns (Voronoi of random points on a torus, ≥ 10 instances, 20–200 faces per cell) with σ from Eq.(1). Use the periodic boundary rows (Eq. 3b–c) and report dim of the null space per pattern.

Claims to test:
C1 (Jacobian affinity): with translation vectors p_x(θ), p_y(θ) from FK, P_θ = [p_x p_y], J(θ) = P_θ P_0⁻¹. Prediction: J(θ) = cos(θ/2) I + sin(θ/2) K(X) exactly, K constant in θ and LINEAR (affine) in X over the null space X = X0 + Φt. Test: fit K from two angles, predict J at 50 angles (rel err < 1e-9); check K(X0 + Φt) is affine in t on 1000 random (pattern, t) pairs (second differences < 1e-9). Report dim of the achievable set 𝒦 = {K(X): X ∈ X} (rank of t ↦ K) per pattern, and compare with min(4, 2·dim_null) — state the formula you find.
C2 (conformality exact): for patterns with dim 𝒦 ≥ 1, solve the linear conditions K = scaled rotation (K₁₁ = K₂₂, K₁₂ = −K₂₁) in t; verify conformal distortion of J(θ) < 1e-9 at 200 angles for the solution (this proves 2026 Sec 5.1's 'empirically for all θ' claim numerically); report which patterns admit it.
C3 (designable Poisson family): Poisson ratio ν(θ) along direction d from U = polar stretch of J(θ) (as in IsoGami Sec 4.3 / 2026 Sec 5.1); the achievable ν-curves are parameterized by K ∈ 𝒦. Algorithm: given a target K* (random in 𝒦, and also anisotropic targets like K* = diag(1, −0.5) if achievable), solve the linear system for t (least squares), then choose among the solution affine subspace the member maximizing certified Θ_max (use the certificate from code/src/method; the shape-space search can be a small L-BFGS over the remaining freedom). Report: target hit error, certified validity rate, certified Θ_max, and ν(θ) predicted vs measured by FK at 20 angles (max abs error).
C4 (B20 closed-form second closed angle): total hole area A(θ) = q(cos θ − 1) + r sin θ (from T3); predicted second gap-free angle θ_c = 2·atan2(r, q) (check the formula from persona_optimizer idea 5, derive it yourself and state it); for patterns with a second closed state (2026 Sec 5.2 examples: rotating squares θ = π, etc.), compare θ_c with the measured angle where hole area vanishes (shoelace on FK, bisection), and check whether the closed state is overlap-free (2026's equal-length condition). Also test the periodic version θ_c = 2·atan(tr K / (1 − det K)) if you can derive it, else report as not derived.
PASS rules: C1 affine and exact (all tolerances met on ≥ 30 patterns); C3 target-hit < 1e-8 with certified valid, Θ_max > 0 designs on ≥ 80% of patterns with dim 𝒦 ≥ 1; C4 |θ_c − measured| < 1e-6 on every pattern with a second closed state. Report FAIL honestly with the failing pattern saved.
Write results/kill/k7/ CSV + PNGs (ν(θ) curves predicted vs measured for 4 patterns; achievable-K dimension histogram) and the K7 section. Do not commit. Reply with the verdicts and numbers in ≤ 12 lines.
