# Ideation round 2 — new theorems/algorithms extending the arbitrary-planar-graph kirigami framework, informed by everything measured in round 1

Format and hard constraints: exactly as specs/ideator.md (10 ideas, nine fields each, self-attack, ranked list), MISSION §3/§8 constraints unchanged. Output: ideas/round2_<PERSONA>.md.

## What is now KNOWN that round 1 did not know (read STATE.md F14–F30, D5–D9; results/kill/KILL_REPORT.md; derivations/core.md T1–T7 statements; derivations/check.md verdicts; notes/repo_2025.md if present; docs/run_notes.pdf §6–§9 if you want the narrative)
1. Uniform deployment is trig-linear: Y_θ = cos(θ/2)C(X) + sin(θ/2)S(X); every contact predicate is p + q cos θ + r sin θ with p,q,r quadratic in X; exact Θ_max and a certificate POS ∧ NOOVERLAP(ε/2) ∧ NOROOT are implemented and validated (K1b, K2a); three deflation classes; grazes handled. This machinery is AVAILABLE in code/src/method and cheap (ms–s per design).
2. The 2026 'full design space' is generically EMPTY of deployable embeddings on random planar graphs: Eq.(6) 0/200, max-cut and defect-minimizing σ 0/200, 0⁺ feasibility repair 0/400 (K1a, K5, K6). Mechanism: inward-translating split-cut duplicates and vertex-into-edge contacts at θ=0⁺. Authored tilings are fine (0/8 fail). Split cuts are the only source of design freedom (dim X = |E_split|) AND the only source of collision loss (Θ_max = min(min β, π) iff no split cuts).
3. Mobility of the cut structure is large (median ≈ 300 after 2-core on Delaunay patches); uniform deployment is a special-position 1-parameter branch inside a high-dimensional configuration space; A(θ)σ = 0 for all θ (no locking).
4. Rank corrections: L = R·D; periodic rank(L) = H − 1; H = |E_hinge| − |F| + c(Γ); the authors' code over-constrains boundary notches.
5. The authors' collision optimizer silently re-closes on 9.5% of designs; ours cannot beat it on range (K2b).
6. K7 (periodic Jacobian affinity / designable Poisson family / closed-form second closed angle) is running; interim: C3 (target-driven design) at 6/22 — the affinity theorem may hold while the design algorithm underperforms.
7. The 2025 paper's open problems (notes/paper_2025.md Sec 13–15): non-uniform deployment theory (Fig. 10), realizable shape space per pattern (triangles cannot close a hemisphere, Fig F.4), automatic tiling selection from curvature, singularity placement. Its code and data (16 patterns, target meshes) are at baseline/kirigami_tessellations.

## What we want from round 2
Ideas that turn the above into a THEOREM, CHARACTERIZATION, or ALGORITHM (MISSION §8) that could be demonstrated within hours on top of the existing code. Especially: (a) an existence / non-existence criterion for usable embeddings (why tilings work and random graphs do not; a combinatorial or geometric condition on (G, σ)); (b) graph classes or orientation rules that provably avoid inward split cuts (e.g. constraints tying split-edge translation direction to the hinge in-neighbour barycentric structure); (c) the realizable shape space / 'curvature budget' of a pattern from the hole-area harmonic at θ_max, tested against the 2025 successes/failures; (d) non-uniform deployment theory (angle fields, the mobility pencil, bifurcations at the flat state) for deployment-unfriendly tilings; (e) inverse design with certified deployment (replace simulation checks in both papers' pipelines); (f) anything else that survives the adversary. Explicitly list, for each idea, which measured fact motivates it and which measured fact could kill it in < 30 min using the existing drivers (kill_k*.cpp, kiri_analyze, certificate API).
