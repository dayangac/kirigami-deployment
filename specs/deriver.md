# Deriver: core derivation for R1 "the usable shape space"

Output: derivations/core.md (step-numbered; every step either a definition, a cited fact, or an algebraic step that a Checker can verify by hand or by a numeric test). No code, except that you MAY write small throwaway numeric checks in C++ under derivations/scratch/ to sanity-test a formula (state which you ran).

Read first: specs/common_preamble.md; STATE.md (F1–F22, U3–U9, D5); ideas/ranking.md Secs 2.3 and 3 (R1, R2) and Sec 4 (K1/K2); ideas/persona_geometer.md (G1, G3 derivations), ideas/persona_rigidity.md (idea 1, 2, no-locking discussion), ideas/persona_adversary.md (Part on (b) contact ≠ collinearity, no-locking lemma, A1 separation 2t(R+Pt)); results/kill/KILL_REPORT.md (measured facts the derivation must be consistent with); code/README.md (conventions: σ=+1 clockwise, hinge at source, FK sign convention, how Y_θ is computed by BFS over the hinge graph).

Derive, in this order, with numbered steps and explicit hypotheses:
T1 (Trig-linear deployment). For a uniformly deployable cut structure with face 2-colouring σ on the hinge graph Γ (connected), fixing face f0, every face f rotates by ω_f = σ(f)σ(f0)·θ/2 (or the equivalent under the code's sign convention — state which and prove the equivalence), and every M′-vertex position is Y_θ = cos(θ/2)·C + sin(θ/2)·S with C, S ∈ R^{|V′|×2} LINEAR in X (give C and S explicitly as path sums along a spanning tree of Γ, and prove path-independence from Eq. (2)). Corollaries: (i) each vertex traces an ellipse (or circle) — give centre/axes; (ii) split-cut duplicates stay parallel (one line, replacing 2026 Appendix A); (iii) hinge opening angle equals θ at every hinge (consistency with the definition).
T2 (No-locking). Show σ (as a face angular-velocity field) satisfies the infinitesimal closure constraints at every configuration Y_θ, θ ∈ [0, π), so the uniform branch is a smooth 1-parameter motion that never locks; every termination of deployment is a contact event. State precisely what "closure constraints" are (the 2b₁(Γ)×|F| matrix A(θ) of the rigidity persona, or the pin-joint Jacobian) and prove A(θ)σ = 0.
T3 (Harmonic predicates). For any two M′-vertices u, v and edge (v, w): the signed area of triangle (u, v, w) at angle θ equals p + q cos θ + r sin θ with p, q, r explicit quadratic forms in X (write them via C and S). Same for face signed areas (show they are constant in θ) and for the squared distance between two vertices. Give the formulas.
T4 (Contact calculus). Define first contact between two faces f, g along the uniform path as the smallest θ > 0 at which a vertex of one lies on the boundary of the other (vertex-into-edge-interior) or a vertex coincides with a vertex; prove that generic first contact is vertex-into-edge-interior; derive the exact condition: harmonic root of the area predicate AND two interval inequalities that are themselves harmonics (projection parameters). Give the closed-form root(s) of p + q cos θ + r sin θ = 0 (with the tan(θ/2) substitution: quadratic in t). Then θ_max(X) = min over candidate (vertex, edge) pairs of the first admissible root; show this is exact given a complete candidate list, and discuss the boundary case of hinge-adjacent faces (the analytic β_i bound of 2026 §5.2 should fall out as a special case — verify). Define the active set and prove a sufficient locality criterion (swept-disc bound) that makes the candidate list finite and O(n) under a stated hypothesis (path sums bounded by a constant times face circumradius — state it as a hypothesis, do not claim it as a theorem unless you prove it).
T5 (The usable region). Define U(ε) = {X ∈ X : all faces positively oriented at θ = 0 and θ_max(X) ≥ ε}. Prove: (a) positive orientation of all faces at θ=0 is a finite set of quadratic inequalities in the null-space coefficients t (X = X0 + Φ t); (b) θ_max(X) ≥ ε is equivalent to: for every candidate pair, no admissible root in (0, ε) — express this as a finite Boolean combination of polynomial inequalities in t; hence U(ε) is a semialgebraic set. Say explicitly what is and is not a "basic" semialgebraic set here and be honest about the dependence on the candidate list. Also derive: the separation function of the adversary's A1, its expansion 2t(R + P t) in t = tan(θ/2), and hence the exact false-negative condition of 2026 Eq. (9) (first-order barrier passes but re-contact at t* = −R/P).
T6 (Projection / range maximization). Write the optimization the method will solve: maximize a smooth surrogate of θ_max (log-sum-exp softmin of the first admissible roots over the active set) over t, subject to a log-barrier on face signed areas at θ=0, with exact gradients of the roots w.r.t. t via implicit differentiation of the harmonic (give the formula). State what certificate is produced at the end (validity: all signed areas > 0 at θ = 0; range: exact θ_max recomputed with the complete candidate list).
T7 (Rank/periodic corrections, short). Restate F14/F22 as a lemma with proof: L = R·D, leftnull(L) = hole-constant circulations, periodic ⇒ 1ᵀL = 0, rank(L) = H − 1; hole count H = |E_hinge| − |F| + c(Γ).
For every theorem list: hypotheses, what could break (non-generic position, boundary, split-cut cycles, c(Γ) > 1), and a numeric test the Checker should run (with tolerance). Mark every claim you could not fully prove as CONJECTURE. Length as needed (~500–900 lines).
