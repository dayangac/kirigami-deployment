# Builder-Method: the new method — certified usable Tutte auxetic embeddings (R1 + R5)

## D7 UPDATE (read first): after K2b FAILED (the authors' native Eq.(9) already reaches the kinematic ceiling; median gain 0), the headline is NOT range maximization. The deliverable is:
(A) CHARACTERIZATION: exact closed-form deployment range Θ_max(X) and a three-part validity certificate POS ∧ NOOVERLAP(ε/2) ∧ NOROOT for arbitrary-graph kirigami (derivations/core.md T3–T5, with graze handling T4.2″, three-class deflation T5.1c/d/e′, T3.H.1 exclusion, moving-centroid broad phase with exact radius). Must be implemented as a clean library API `exact_theta_max(...)` and `certify(...)` and validated against brute-force bisection.
(B) ALGORITHM (only if K5 passes — read results/kill/KILL_REPORT.md K5): defect-minimizing orientation σ_def, followed by Eq.(6) projection and a SMALL range/validity repair in the null space (keep the range optimizer from the Experimenter's code as an optional post-process, not the headline). Metric = certified validity rate and certified Θ_max, baselines = Eq.(1) σ + Eq.(6) (+ authors' native prevent).
(C) The Eq.(9) false-negative detector: given any embedding claimed collision-free to angle θ_c, report certified re-closure (K1c logic).
Step 3 below is downgraded to optional; steps 1, 2, 4, 5, 6 stand; prioritize (A) and (C) API quality and tests, then (B).


Output: code/src/method/ (namespace kiri::method; extend what the Experimenter started: deploy_basis, mobility, contact, range_opt), app code/apps/kiri_usable.cpp, tests code/tests/test_method.cpp (extend), README section "Method". All C++ (D3).

Read first: specs/common_preamble.md; code/README.md; STATE.md (D5, D6, F15–F26); derivations/core.md (T1 trig-linear basis, T3 harmonics, T4 contact calculus incl. graze handling and moving-centroid broad phase with exact radius max(‖x‖,‖χ‖), T5 usable region and three-item validity certificate, T6 gradients) and derivations/check.md (what was corrected); results/kill/KILL_REPORT.md (K1a emptiness, K5 σ-defect result, K2b margin, K2c candidate-set growth, K1c, K2a agreement rates); ideas/ranking.md R1/R5.

The method, as one pipeline `usable_embedding(mesh, X_ini, params) -> {σ, X, certificate}`:
1. ORIENTATION: σ by local search minimizing the deployability defect D(σ) = Σ_holes ‖Σ_{hinge e∈C}(x_t − x_s)‖² at X_ini (single-face and adjacent-pair flips; reject c(Γ) > 1 or detached faces; restarts from Eq.(1) σ and random σ; deterministic seed). Expose Eq.(1) max-cut σ as the baseline option.
2. PROJECTION: Eq.(6) X0 = proj(X_ini) onto X_σ and the null basis Φ (existing core).
3. RANGE-OPTIMAL VALID EMBEDDING: maximize over t (X = X0 + Φt) a smooth surrogate of θ_max (log-sum-exp softmin of first admissible contact roots over the active set, T4.2″ with crossing/interval clauses, T6.2 gradients) with a log-barrier on face signed areas at θ=0 and a penalty/barrier keeping X near X_ini (relative edge-length change); L-BFGS with line search; active set refreshed every k iterations from the moving-centroid broad phase; τ = tan(θ/2) domain θ ∈ (0, π).
4. CERTIFICATE: (i) all face signed areas > 0 at θ=0; (ii) no face–face overlap at θ=0 (collision.hpp exact test); (iii) exact θ_max recomputed with the COMPLETE candidate list (no pruning) and cross-checked against bisection (tol 1e-5); output {valid: bool, theta_max, binding pair, certificate details} as JSON.
5. Baselines for comparison, callable from the same CLI: (a) Eq.(1) σ + Eq.(6) only; (b) (a) + our collision.hpp Eq.(9); (c) (a) + the authors' native `prevent` via baseline/native/build/tuttekiri_cli (subprocess; parse its JSON; then re-certify with OUR certificate). Metrics per graph: valid?, θ_max(certified), inverted faces, max relative edge-length change vs X_ini, |E_split|, D(σ), wall time.
6. CLI: `kiri_usable <graph.json> [--sigma defect|maxcut|json] [--eps 0.3] [--out dir]` writing σ, X, certificate, and deployment snapshots (θ = 0, θ_max/2, θ_max) as JSON + a PNG via `arch -arm64 /usr/local/bin/python3` (reuse code/scripts/plot_embedding.py).
Tests (doctest, register in CMakeLists): certificate agrees with bisection on 100 random valid embeddings; the σ search never returns c(Γ) > 1; on the 8 reference cases the method's θ_max ≥ the authors' prevent result (or document where not); gradient of the softmin vs finite differences (rel err < 1e-5, 1000 samples); graze case (hexagons_auto) handled: θ_max = 2.094 not 1.047. All existing tests must still pass. Do not commit. Reply with: CLI usage, the 8-reference-case table (ours vs baselines a/b/c), and any case where ours loses.
