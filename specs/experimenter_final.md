# Experimenter (Phase 9): final comparison, ablations, hero example

Output: results/final/ (CSV/JSON, PNG figures via `arch -arm64 /usr/local/bin/python3`, FINAL_REPORT.md), plus the hero example's inputs and exports (via kiri_export) under export/hero/.
Read first: specs/common_preamble.md; STATE.md (D7, F25–F29, K5 outcome); results/kill/KILL_REPORT.md; code/README.md (Method section from Builder-Method); derivations/core.md T4–T5 (what is claimed exactly).

Experiments (all C++; ≥ 20 graphs everywhere, ≥ 500 for the characterization validation):
E1 Characterization validation (MISSION §8b): exact Θ_max (T4.2″) vs brute-force bisection (collision.hpp, tol 1e-5) on ≥ 500 random graphs (Voronoi/Delaunay/quad-random, 100–5000 faces, σ from Eq.(1) AND σ_def if K5 passed) at ≥ 3 embeddings each (X_ini-projected, random null-space points). Report agreement rate, worst error, wall time of exact vs bisection, and the rate at which naive min-over-roots is wrong. Also validate the certificate: whenever certify(X, ε) says valid, brute-force confirms no overlap on a 200-angle sweep of (0, ε); whenever it says invalid, show the binding pair and confirm by bisection.
E2 Emptiness vs usability: the K1a protocol on ≥ 200 graphs with σ_mc vs σ_def (if K5 passed): certified validity rate, projection distance, |E_split|, D(σ); plus the 8 authored tilings (all valid) as the contrast the paper's figures show. Figure: validity rate by generator and by |E_split|/|F|.
E3 Eq.(9) false-negative rate of the authors' native prevent on ≥ 100 designs (K1c logic), with the distribution of re-closure angles relative to the claimed range.
E4 Ablations: (i) graze handling on/off; (ii) deflation classes on/off (count spurious roots); (iii) broad phase on/off (candidate count, time); (iv) σ search restarts 1 vs 4 (if K5 passed).
E5 Hero example: choose one non-2-colourable, aperiodic random graph (≥ 300 faces) where σ_mc + Eq.(6) is invalid and (if K5 passed) σ_def + Eq.(6) (+ repair) is certified valid with a large certified Θ_max; if K5 failed, choose one where the certificate exposes a false collision-free claim of the authors' prevent. Produce: figure strip (X_ini, Eq.(6) X0 with inverted/overlapping faces highlighted, ours, deployment at Θ_max/2 and Θ_max), and exports via kiri_export (closed + open SVG, STL, 3MF) into export/hero/.
E6 Scaling: wall time of the exact Θ_max and certificate vs |F| up to 5000 faces (log-log plot).
FINAL_REPORT.md: every table with the exact command that produced it; PASS/FAIL against the claims in STATE.md D7; failure cases listed explicitly. All existing tests must pass. Do not commit. Reply with the headline numbers and the hero example path in ≤ 20 lines.
