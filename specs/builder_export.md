# Builder-Export: fabricable export (SVG for laser, 3MF/STL for print) with a parametric hinge model

Output: code/src/export/ (namespace kiri::export_), app code/apps/kiri_export.cpp, tests in code/tests/test_export.cpp, docs appended to code/README.md (section "Export"). All C++ (D3). Another agent is concurrently editing code/apps and code/tests for experiments: do NOT modify existing files except (a) appending ONE `add_subdirectory(src/export)` line and the two target lines for your app/test to code/CMakeLists.txt using an exact-match edit, and (b) appending a section to code/README.md. Put your own CMake logic in code/src/export/CMakeLists.txt.

Read first: specs/common_preamble.md; code/README.md (API: Mesh, CutStructure, forward kinematics `deploy`, collision/θ_max); STATE.md F1, F6, F18; MISSION §7 (parametric hinge: face extrusion, hinge pad radius, neck width, gap, driven by a material profile struct).

Deliverables:
1. `MaterialProfile` struct: sheet thickness, kerf, hinge type ∈ {living-hinge-neck, pin-pad}, neck width, pad radius, face inset/gap, min feature size; presets: "felt_laser" (2 mm felt, neck 1.5 mm, gap 0.3 mm), "paper_laser" (0.3 mm card, neck 1.0 mm), "pla_print" (2 mm, pin-pad, pad r 2.5 mm).
2. SVG writer (closed state, θ = 0, and any deployed state θ): faces as closed paths inset by gap/2 and kerf/2 around every cut edge; hinge cuts rendered as a partial cut leaving a neck of the given width centred on the hinge (source) vertex, split cuts as full cuts; boundary uncut; separate layers (SVG groups with ids) for "cut", "score", "engrave" (engrave: σ arrows like 2025 Fig 1, and face ids); units mm; viewBox correct; valid XML. For the deployed state, faces are placed by our forward kinematics and holes are visible.
3. 3MF and STL writers (3D): each face extruded by thickness, with pin-pad hinges (cylindrical pad on one face, matching hole with clearance on the neighbour, at each hinge vertex, alternating up/down so adjacent faces overlap only at the pad) OR living-hinge necks (thin bridge). Manifold, consistently oriented triangles; STL binary; 3MF = zip with 3D/3dmodel.model + [Content_Types].xml + _rels/.rels (write the zip with a minimal store-only zip writer — no external libs). Validate by re-reading the STL and checking closedness (every edge shared by exactly 2 triangles) and by checking the 3MF opens in a slicer if one is installed (check `which prusa-slicer bambu-studio cura` — if none, state so and validate XML structure + zip integrity with `unzip -t`).
4. CLI: `kiri_export <graph.json> --profile felt_laser --theta 0|<rad> --svg out.svg --stl out.stl --3mf out.3mf [--embedding X.json]`.
5. Tests (doctest, standalone-buildable like the others; register in your CMakeLists): SVG parses as XML (write a tiny well-formedness check), path count equals face count, neck present at every hinge vertex and absent at split edges, STL closed and consistently oriented, 3MF unzips and its model XML lists the expected number of objects, bounding boxes match the input embedding scaled to mm.
6. Produce demo exports for two reference cases (results/core_validation/cases/snub_square_33434 and tiling_3_4_3_12): closed (θ=0) and open (θ = 0.8·θ_max) as SVG, plus STL/3MF for the closed state, into export/samples/. Render PNG previews of the SVGs with `arch -arm64 /usr/local/bin/python3` (matplotlib, parse the paths yourself or via svgpathtools if installed — if not, draw from the same face polygons) into export/samples/*.png.
Build: `cmake -S code -B code/build -DCMAKE_OSX_ARCHITECTURES=arm64 && cmake --build code/build -j && ./code/build/kiri_tests` must still pass, plus your own test binary. Do not commit. Reply with the file list, test results, and the slicer/validation status in ≤ 15 lines.
